module.exports = Object.freeze({
  ctrl: require('./contractTransporterBid.controller'), // exporting the controller 
  routes: require('./contractTransporterBid.router') // exporting the routes 
});
